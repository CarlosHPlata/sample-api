name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  content: read

concurrency:
  grou: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: "checkout code"
        uses: actions/checkout@v5

      - name: "Setup node js"
        uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: "install dependencies"
        run: npm ci

      - name: "typecheck"
        run: npx tsc --noEmit -p tsconfig.json

      - name: "tests"
        run: "npm test"
